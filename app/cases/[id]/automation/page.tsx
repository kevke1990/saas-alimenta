import Link from "next/link";import AppShell from "@/components/AppShell";import {requireUser} from "@/lib/auth";import {db} from "@/lib/db";import CaseAutomation from "@/components/CaseAutomation";
export default async function AutomationPage({params}:{params:Promise<{id:string}>}){const u=await requireUser();const{id}=await params;const c=await db.case.findFirst({where:{id,userId:u.id},select:{id:true,name:true}});if(!c)return <AppShell><div className="notice error">Dossier niet gevonden.</div></AppShell>;return <AppShell><div className="page-head"><div><div className="eyebrow">Fase F · F3 Automatisering</div><h1 className="page-title">{c.name}</h1><p className="page-subtitle">Gecontroleerde opvolging vanuit de dossiercontext.</p></div><div className="actions"><Link className="btn secondary" href={`/cases/${id}`}>← Dossier</Link><Link className="btn secondary" href={`/cases/${id}/workflow`}>Workflow</Link></div></div><CaseAutomation caseId={id}/></AppShell>}
