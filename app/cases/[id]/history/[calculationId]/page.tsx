import Link from 'next/link';
import AppShell from '@/components/AppShell';
import { requireUser } from '@/lib/auth';
import { db } from '@/lib/db';
const money=(v:any)=>new Intl.NumberFormat('nl-NL',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(Number(v)||0);
const date=(v:any)=>new Intl.DateTimeFormat('nl-NL',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v));
export default async function CalculationSnapshotPage({params}:{params:Promise<{id:string;calculationId:string}>}){const u=await requireUser();const {id,calculationId}=await params;const c=await db.case.findFirst({where:{id,userId:u.id},include:{calculations:{where:{id:calculationId},take:1}}});if(!c||!c.calculations[0])return <AppShell><div className="notice error">Snapshot niet gevonden.</div></AppShell>;const x:any=c.calculations[0],r:any=x.result||{},data:any=x.inputSnapshot||{},transfers=r.transfers||[];const ka=transfers.reduce((s:number,t:any)=>s+Number(t.payment||0),0);const pal=Number(r.partnerSupport?.result?.monthlyGross||r.combined?.partnerSupport?.monthlyGross||0);return <AppShell><main className="content" style={{maxWidth:1100}}><div className="page-head"><div><div className="eyebrow">Immutable snapshot</div><h1 className="page-title">Berekening van {date(x.createdAt)}</h1><p className="page-subtitle">Engine {x.engineVersion} · norm {x.normVersion}. Deze pagina is alleen-lezen.</p></div><div className="actions"><Link className="btn secondary" href={`/cases/${id}/history`}>← Historie</Link><Link className="btn" href={`/cases/${id}/edit`}>Huidige berekening wijzigen</Link></div></div><div className="result-overview"><div className="result-metric"><div className="stat-label">KINDERALIMENTATIE</div><div className="metric-value">{money(ka)}</div><span>per maand</span></div><div className="result-metric"><div className="stat-label">PARTNERALIMENTATIE</div><div className="metric-value">{money(pal)}</div><span>bruto per maand</span></div><div className="result-metric"><div className="stat-label">BEHOEFTE</div><div className="metric-value">{money(r.totalNeed)}</div><span>basisberekening</span></div><div className="result-metric"><div className="stat-label">DRAAGKRACHT</div><div className="metric-value">{money(r.totalCapacity)}</div><span>basisberekening</span></div></div><section className="panel topgap"><div className="panel-head"><div><h2 className="panel-title">Snapshotdetails</h2><div className="panel-sub">De oorspronkelijke invoer en uitkomst zijn bewaard voor audit en vergelijking.</div></div></div><div className="detail-grid"><div className="detail-card"><div className="summary-line"><span>Snapshot-ID</span><b>{x.id}</b></div><div className="summary-line"><span>Aangemaakt</span><b>{date(x.createdAt)}</b></div><div className="summary-line"><span>Engineversie</span><b>{x.engineVersion}</b></div><div className="summary-line"><span>Normversie</span><b>{x.normVersion}</b></div></div><div className="detail-card"><div className="summary-line"><span>Aantal ouders</span><b>{data.parents?.length||0}</b></div><div className="summary-line"><span>Aantal kinderen</span><b>{data.children?.length||0}</b></div><div className="summary-line"><span>PAL actief</span><b>{data.partnerSupport?.enabled?'Ja':'Nee'}</b></div><div className="summary-line"><span>Totale maandbetaling</span><b>{money(ka+pal)}</b></div></div></div></section><section className="panel topgap"><div className="panel-head"><div><h2 className="panel-title">Audit-opmerking</h2></div></div><div className="notice">Deze snapshot wordt niet gewijzigd. Gebruik <b>Berekening wijzigen</b> om het hoofddossier opnieuw te berekenen; iedere herberekening krijgt vervolgens een nieuw historisch snapshot.</div></section></main></AppShell>}
