import Link from "next/link";
import AppShell from "@/components/AppShell";
import DocumentUploadForm from "./DocumentUploadForm";
import {requireUser} from "@/lib/auth";
import {db} from "@/lib/db";
const labels:Record<string,string>={NOT_ANALYZED:"Niet geanalyseerd",PROCESSING:"Bezig",COMPLETED:"Geanalyseerd",ANALYZED:"Geanalyseerd",FAILED:"Analyse mislukt",DISABLED:"AI uitgeschakeld",SKIPPED:"Overgeslagen",TOO_LARGE:"Te groot",RATE_LIMITED:"AI-limiet"};
const categoryLabel=(v:string)=>({IDENTITEIT:"Identiteit",INKOMEN:"Inkomen",ONDERNEMING:"Onderneming",WONEN:"Wonen",KINDEREN:"Kinderen",ZORGREGELING:"Zorgregeling",JURIDISCH:"Juridisch",BEREKENINGEN:"Berekeningen",FACTUREN:"Facturen",OVERIG:"Overig"}[v]||v);
export default async function DocumentsPage({params}:{params:Promise<{id:string}>}){const u=await requireUser();const{id}=await params;const c=await db.case.findFirst({where:{id,userId:u.id},include:{client:true,documents:{orderBy:{createdAt:"desc"},include:{incomeFacts:{orderBy:{createdAt:"asc"}}}}}});if(!c)return <AppShell><div className="notice error">Dossier niet gevonden.</div></AppShell>;const facts=c.documents.flatMap(d=>d.incomeFacts);const proposed=facts.filter(f=>f.status==="PROPOSED").length;const approved=facts.filter(f=>f.status==="APPROVED").length;const parentName=(i:number|null|undefined)=>i===0?(c.client?.personAName||"Ouder 1"):i===1?(c.client?.personBName||"Ouder 2"):"Nog niet gekoppeld";
return <AppShell><div className="page-head"><div><div className="eyebrow">Fase B · documenten</div><h1 className="page-title">Documenten & gegevensinname</h1><p className="page-subtitle">{c.name} · {c.client?.name||"Zonder cliënt"}</p></div><div className="actions"><Link className="btn secondary" href={`/cases/${id}`}>← Dossier</Link><Link className="btn secondary" href={`/cases/${id}/income-facts`}>Fact review</Link></div></div><div className="result-hero"><div className="stat-card result-main"><div className="stat-label">DOCUMENTEN</div><div className="stat-value">{c.documents.length}</div><div className="stat-meta">Veilig opgeslagen</div></div><div className="stat-card"><div className="stat-label">AI-VOORSTELLEN</div><div className="stat-value">{proposed}</div><div className="stat-meta">Wachten op beoordeling</div></div><div className="stat-card"><div className="stat-label">GOEDGEKEURD</div><div className="stat-value">{approved}</div><div className="stat-meta">Professioneel bevestigd</div></div></div><section className="panel topgap"><div className="panel-head"><div><div className="section-kicker">Nieuwe documentinname</div><h2 className="panel-title">Bestand uploaden</h2><div className="panel-sub">Categorie en ouder kunnen direct worden vastgelegd. AI-extractie blijft altijd een voorstel.</div></div></div><DocumentUploadForm caseId={id} clientId={c.clientId}/></section><section className="panel topgap"><div className="panel-head"><div><div className="section-kicker">Documentregister</div><h2 className="panel-title">Dossierdocumenten</h2><div className="panel-sub">Documenten zijn geordend op categorie en kunnen aan een ouder worden gekoppeld.</div></div></div>{c.documents.length===0?<div className="empty">Nog geen documenten toegevoegd.</div>:<div className="table-wrap"><table className="table"><thead><tr><th>Document</th><th>Categorie</th><th>Ouder</th><th>AI-status</th><th>Feiten</th></tr></thead><tbody>{c.documents.map(d=><tr key={d.id}><td><a className="table-link" href={`/api/documents/${d.id}`} target="_blank" rel="noreferrer">{d.name}</a><div className="table-note">{d.sizeBytes.toLocaleString("nl-NL")} bytes · {new Date(d.createdAt).toLocaleString("nl-NL")}</div></td><td>{categoryLabel(d.category)}</td><td>{parentName(d.parentIndex)}</td><td><span className={`status ${d.aiStatus==="COMPLETED"||d.aiStatus==="ANALYZED"?"green":d.aiStatus==="FAILED"?"red":"amber"}`}>{labels[d.aiStatus]||d.aiStatus}</span></td><td>{d.incomeFacts.length}{d.incomeFacts.length>0&&<div className="table-note">{d.incomeFacts.filter(f=>f.status==="PROPOSED").length} voorstellen</div>}</td></tr>)}</tbody></table></div>}</section><section className="panel topgap"><div className="panel-head"><div><div className="section-kicker">Workflow</div><h2 className="panel-title">Document → AI → professional → berekening</h2></div></div><div className="notice success">Document veilig opslaan → extractie → AI-voorstel → ouder toewijzen → professionele goedkeuring → verwerking in de bestaande berekening → nieuwe calculation snapshot.</div><div className="actions topgap"><Link className="btn" href={`/cases/${id}/income-facts`}>Open professionele fact review →</Link><Link className="btn secondary" href={`/cases/${id}/review`}>Open volledige review →</Link></div></section></AppShell>;
}
