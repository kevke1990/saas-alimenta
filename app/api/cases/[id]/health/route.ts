import {NextResponse} from "next/server";import {requireUser} from "@/lib/auth";import {db} from "@/lib/db";import {assessDossierHealth} from "@/lib/dossier-health";
export async function GET(_req:Request,{params}:{params:Promise<{id:string}>}){try{const u=await requireUser();const {id}=await params;const c=await db.case.findFirst({where:{id,userId:u.id},include:{documents:{select:{aiStatus:true,approvedAt:true}},calculations:{select:{id:true}}}});if(!c)return new NextResponse("Dossier niet gevonden",{status:404});return NextResponse.json(assessDossierHealth({data:c.data,documents:c.documents,calculations:c.calculations.length}));}catch(e:any){return new NextResponse(e?.message||"Dossiercontrole mislukt",{status:400})}}
