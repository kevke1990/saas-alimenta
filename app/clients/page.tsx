import Link from "next/link"; import AppShell from "@/components/AppShell"; import { requireUser } from "@/lib/auth"; import { db } from "@/lib/db";
export default async function Clients(){const u=await requireUser();const clients=await db.client.findMany({where:{userId:u.id,status:"ACTIVE"},include:{_count:{select:{cases:true}}},orderBy:{name:"asc"}});return <AppShell><div className="page-head"><div><div className="eyebrow">Relaties</div><h1 className="page-title">Cliënten</h1><p className="page-subtitle">Beheer je cliënten en de bijbehorende alimentatiedossiers.</p></div><Link className="btn" href="/clients/new">+ Nieuwe cliënt</Link></div><section className="panel">{clients.length===0?<div className="empty">Nog geen cliënten toegevoegd.</div>:<table className="table"><thead><tr><th>Cliënt</th><th>Klantnummer</th><th>Contact</th><th>Dossiers</th><th></th></tr></thead><tbody>{clients.map(c=><tr key={c.id}><td><Link className="table-link" href={`/clients/${c.id}`}>{c.name}</Link></td><td>{c.reference||"—"}</td><td>{c.email||c.phone||"—"}</td><td>{c._count.cases}</td><td><Link className="table-link" href={`/clients/${c.id}`}>Open →</Link></td></tr>)}</tbody></table>}</section></AppShell>}
