"use client";
import Link from "next/link"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function NewClient(){const [f,setF]=useState({name:"",reference:"",email:"",phone:"",notes:""});const [err,setErr]=useState("");const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setErr("");const x=await fetch("/api/clients",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)});if(x.ok){const c=await x.json();r.push(`/clients/${c.id}`)}else setErr(await x.text())}return <main className="content" style={{maxWidth:900}}><div className="page-head"><div><div className="eyebrow">Nieuwe relatie</div><h1 className="page-title">Cliënt toevoegen</h1><p className="page-subtitle">Leg de basisgegevens vast. Je kunt daarna direct een dossier starten.</p></div><Link className="btn secondary" href="/clients">Annuleren</Link></div><section className="panel"><form onSubmit={submit}><div className="form-grid">{[["name","Naam","Bijv. Jan de Vries"],["reference","Klantnummer","Bijv. CLI-2026-014"],["email","E-mailadres","naam@voorbeeld.nl"],["phone","Telefoonnummer","06 12 34 56 78"]].map(([k,l,p])=><div key={k}><label className="label">{l}</label><input required={k==="name"} className="input" placeholder={p} value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></div>)}<div className="full"><label className="label">Interne notities</label><textarea className="input" rows={6} placeholder="Alleen zichtbaar binnen je praktijk." value={f.notes} onChange={e=>setF({...f,notes:e.target.value})}/></div></div>{err&&<div className="notice error topgap">{err}</div>}<div className="actions topgap"><button className="btn">Cliënt opslaan</button><Link className="btn secondary" href="/clients">Annuleren</Link></div></form></section></main>}
