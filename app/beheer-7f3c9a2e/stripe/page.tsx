"use client";
import {useEffect,useState} from "react";
export default function StripeAdmin(){const [data,setData]=useState<any>();const [key,setKey]=useState("");const [wh,setWh]=useState("");const [msg,setMsg]=useState("");const load=()=>fetch("/api/beheer-7f3c9a2e/stripe").then(r=>r.json()).then(setData);useEffect(()=>{void load()},[]);async function act(action:string){setMsg("Bezig…");const r=await fetch("/api/beheer-7f3c9a2e/stripe",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,secretKey:key,webhookSecret:wh})});setMsg(await r.text());if(r.ok){setKey("");setWh("");await load()}}return <main className="container"><div className="card"><h1>Stripe beheer</h1><p className="muted">Configureer Stripe volledig vanuit Alimenta Pro. Secrets worden versleuteld opgeslagen.</p><h2>1. Stripe koppelen</h2><label className="label">Secret key</label><input className="input" type="password" value={key} onChange={e=>setKey(e.target.value)} placeholder="sk_test_… of sk_live_…"/><label className="label">Webhook signing secret</label><input className="input" type="password" value={wh} onChange={e=>setWh(e.target.value)} placeholder="whsec_…"/><button className="btn" onClick={()=>act("saveCredentials")}>Opslaan</button><button className="btn secondary" onClick={()=>act("test")}>Verbinding testen</button><p>{msg}</p><div className="notice"><b>Webhook URL</b><br/>{data?.webhookEndpoint}</div><h2>2. Producten & jaarprijzen</h2><p>Maak/controleer de vier producten en prijzen automatisch in de actieve Stripe-modus.</p><button className="btn" onClick={()=>act("syncProducts")}>Stripe-producten synchroniseren</button><table className="table"><thead><tr><th>Plan</th><th>Prijs</th><th>Stripe Price</th></tr></thead><tbody>{data?.plans?.map((p:any)=><tr key={p.key}><td>{p.name}</td><td>€{(p.annualAmountCents/100).toFixed(2)} / jaar</td><td>{p.stripePriceId||"—"}</td></tr>)}</tbody></table><h2>3. Klantbeheer</h2><p>Klanten beheren hun betaalmethode, abonnement en facturen via de Stripe Customer Portal vanuit <code>/billing</code>.</p></div></main>}
